---
import { Earth, Network, Receipt } from "@lucide/astro";

const framework = [
  {
    icon: Network,
    title: "Predictive Analytics",
    text: `Moving beyond historical data, we utilize advanced predictive modeling
           and AI-driven tools to anticipate market shifts and identify future
           trends. This allows our clients to prepare for global economic
           fluctuations proactively, rather than reacting to them.`,
  },
  {
    icon: Earth,
    title: "Cross-Border Finance",
    text: `Our deep expertise in international taxation, transfer pricing
           protocols, and FEMA compliance ensures your capital flows efficiently
           across borders. We navigate complex regulatory environments to
           minimize friction and optimize global operations.`,
  },
  {
    icon: Receipt,
    title: "Standardized Reporting",
    text: `We harmonize your financial language with global standards (IFRS, US
           GAAP, Ind AS), providing clear, comparable, and transparent numbers.
           This builds trust with international investors, stakeholders, and
           regulatory bodies worldwide.`,
  },
];
---

<section class="relative space-y-20 bg-white px-8 py-10 md:px-16 md:py-20">
  <div class="mx-auto flex max-w-3xl flex-col gap-6 text-center">
    <span
      class="text-brand-700 mb-3 text-sm font-bold tracking-wider uppercase"
    >
      Our Framework
    </span>

    <h2 class="text-4xl font-bold text-gray-900">
      Navigating the Global Financial Landscape
    </h2>

    <p class="text-lg text-gray-600">
      We operate at the intersection of data, finance, and global standards to
      provide a holistic view of your business's potential.
    </p>
  </div>

  <div class="grid grid-cols-1 gap-8 md:grid-cols-3">
    {
      framework.map((item) => {
        const Icon = item.icon;
        return (
          <div class="group bg-white text-center">
            <div class="border-brand-50 text-brand-700 group-hover:border-brand-200 mx-auto mb-8 flex size-24 items-center justify-center rounded-full border-4 bg-white text-4xl shadow-sm transition-all group-hover:shadow-md">
              <Icon />
            </div>

            <h4 class="mb-4 text-2xl font-bold text-gray-900">{item.title}</h4>

            <p class="px-4 leading-relaxed text-gray-600">{item.text}</p>
          </div>
        );
      })
    }
  </div>
</section>
